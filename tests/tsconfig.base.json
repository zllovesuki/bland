{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    // Keep test typechecking separate from the app build. This lets the main
    // project continue excluding tests while still giving tests the same alias
    // and module-resolution behavior as src/.
    "rootDir": "..",
    "paths": {
      "@/*": ["../src/*"],
      "@tests/*": ["./*"]
    }
  }
}
